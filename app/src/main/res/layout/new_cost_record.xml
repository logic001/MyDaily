<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">


    <EditText
        android:id="@+id/et_new_costTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="CostTitle"
        android:layout_margin="4dp" />

    <EditText
        android:id="@+id/et_new_costMoney"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="CostMoney"
        android:layout_margin="4dp"
        android:layout_below="@id/et_new_costTitle"/>

    <DatePicker
        android:id="@+id/dp_new_costDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/et_new_costMoney"
        android:layout_centerInParent="true"
        android:datePickerMode="spinner"
        android:calendarViewShown="false"></DatePicker>
</RelativeLayout>